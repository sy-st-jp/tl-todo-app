import {StatusMessage} from "@/features/todo/hooks/useTodo/shared/types/StatusMessage";

export const STATUS_MESSAGE: StatusMessage = {
    default: "タスク一覧の取得に失敗しました。解決しない場合はこちらにお問い合わせください。(https://example.com)"
}
